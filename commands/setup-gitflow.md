---
description: "Set up Git workflow (use /ypm:git-workflow-setup instead)"
---

This command has been replaced by the `git-workflow-setup` skill.

Use `/ypm:git-workflow-setup` or say "set up git flow".

The skill provides the same functionality with better template management.
